module.exports.help=function()
{
    let a=4;
    console.log(`List of all commands are ${a}
                1. View src -t : to view a directory as tree
                2. view src -f: to view a directory as flat file 
                3. view untreefy src dest :to untree src files to dest
                4. view treefy src dest :inverse of untreefy`);
}